// using slice method to modify arrays
'use strict';
let num = 1;
let arr1 = [1, 2, 3]
let arr2 = ['cold', 'rainy', 'warm'];
let newArray = [];
let str = '';

str = arr1.slice(0, arr1.length)

console.log(str);
